# RDL Table T03 Caption v1.0

Artefact status: diagnostic output.

**Execution-schedule conventions and their completion-impact implications.**
At fixed operational rate, or fixed participation with fixed background flux,
duration is proportional to signed volume and the undamped local-linear Abel
benchmark gives square-root completion scaling. Holding the operational
horizon fixed instead makes the rate proportional to volume and produces the
explicit slope-one negative control. The finite-width child-order row is an
origin-regularised response illustration and carries no completion-scaling
claim. Sources: F05 and F07 designs and diagnostics; staged by
`scripts/RDL-TAB.py`.
